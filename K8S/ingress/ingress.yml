# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: inventory-ingress
#   namespace: inventory-prod
#   annotations:
#     # ── Ingress controller ──
#     kubernetes.io/ingress.class: "nginx"
#     cert-manager.io/cluster-issuer: "letsencrypt-prod"

#     # ── SSL ──
#     nginx.ingress.kubernetes.io/ssl-redirect: "false"
#     nginx.ingress.kubernetes.io/force-ssl-redirect: "false"

#     # ── Rate limiting ──
#     nginx.ingress.kubernetes.io/limit-rps: "20"              # 20 req/sec per IP
#     nginx.ingress.kubernetes.io/limit-connections: "10"       # 10 concurrent per IP

#     # ── Request size ──
#     nginx.ingress.kubernetes.io/proxy-body-size: "10m"        # max upload size

#     # ── Timeouts ──
#     nginx.ingress.kubernetes.io/proxy-connect-timeout: "10"
#     nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
#     nginx.ingress.kubernetes.io/proxy-send-timeout: "60"

#     # ── Security headers ──
#     nginx.ingress.kubernetes.io/configuration-snippet: |
#       more_set_headers "X-Frame-Options: DENY";
#       more_set_headers "X-Content-Type-Options: nosniff";
#       more_set_headers "X-XSS-Protection: 1; mode=block";
#       more_set_headers "Referrer-Policy: strict-origin-when-cross-origin";
#       more_set_headers "Permissions-Policy: camera=(), microphone=(), geolocation=()";

#     # ── CORS ──
#     nginx.ingress.kubernetes.io/enable-cors: "true"
#     nginx.ingress.kubernetes.io/cors-allow-origin: "https://inventory.yourdomain.com"
#     nginx.ingress.kubernetes.io/cors-allow-methods: "GET, POST, PUT, PATCH, DELETE, OPTIONS"
#     nginx.ingress.kubernetes.io/cors-allow-headers: "Authorization, Content-Type"

# spec:
#   # tls:
#   #   - hosts:
#   #       - inventory.local
#   #       - api.local
#   #     secretName: inventory-tls-secret    # must match cert.yaml secretName

#   rules:
#     # ── API traffic ──
#     # - host: api.local
#     - host: inventory.local
#       http:
#         paths:
#           - path: /api/v1
#             pathType: Prefix
#             backend:
#               service:
#                 name: inventory-app-service
#                 port:
#                   number: 3000

#           - path: /metrics
#             pathType: Exact
#             backend:
#               service:
#                 name: inventory-app-service
#                 port:
#                   number: 3000

#     # ── Frontend traffic (if you add one later) ──
#     # - host: inventory.yourdomain.com
#     #   http:
#     #     paths:
#     #       - path: /
#     #         pathType: Prefix
#     #         backend:
#     #           service:
#     #             name: inventory-app-service
#     #             port:
#     #               number: 3000

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: inventory-ingress
  namespace: inventory-prod
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/limit-rps: "20"
    nginx.ingress.kubernetes.io/limit-connections: "10"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"

spec:
  ingressClassName: nginx

  rules:
    - host: inventory.local
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: inventory-app-service
                port:
                  number: 3000

          - path: /metrics
            pathType: Exact
            backend:
              service:
                name: inventory-app-service
                port:
                  number: 3000